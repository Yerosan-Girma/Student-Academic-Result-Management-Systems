PORT=3000
SESSION_SECRET=change_this_secret

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=student_academic_management

# Optional (comma-separated). Only needed if frontend is hosted on a different origin.
# CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500

